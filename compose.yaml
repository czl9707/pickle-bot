services:
  picklebot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ${WORKSPACE_PATH:-~/.pickle-bot}:/root/.pickle-bot
    restart: unless-stopped
